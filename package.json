{
  "name": "vanilla",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/Downey9204/vanillaJS.git",
  "author": "나다운 <devops9204@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "reset-css": "^5.0.1"
  },
  "scripts": {
    "cleanup": "rm -rf .cache dist",
    "dev": "yarn cleanup && parcel index.html --open",
    "build": "parcel build --no-source-maps index.html -d build"
  }
}
